You are Codex working inside this repo.

1) Run `/mcp` and confirm you see:
   - backlog
   - occt-lsp

2) Read:
   - `AGENTS.md`
   - the Backlog.md workflow instructions via `backlog://docs/task-workflow`

3) Open `notes/maps/` and summarize what artifacts exist.
4) Propose the next 10 tasks (Backlog.md tasks) to start OCCT research:
   - 3 mapping/indexing tasks
   - 3 oracle tasks
   - 4 dossier tasks (TopoDS, BRep_Tool, intersections, booleans)

Create the tasks using Backlog.md, each with:
- Description
- Acceptance criteria checklist
- Dependencies (when appropriate)

Do not modify anything under `occt/`.
